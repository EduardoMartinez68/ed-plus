  <table class="table table-hover" id="table-container">
    <colgroup>
        <col style="width: 30%;">
        <col style="width: 30%;">
        <col style="width: 20%;">
        <col style="width: 10%;">
        <col style="width: 10%;">
    </colgroup>
    <thead>
      <tr>
        <th>Nombre <i class="fi-icon fi-sr-restaurant"></i></th>
        <th>correo electronico <i class="fi-icon fi-sr-envelope"></i></th>
        <th>celular/telefono <i class="fi-icon fi-br-smartphone"></i></th>
        <th>puntos<i class="fi-icon fi-sr-coins"></i></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
    {{#each customers}}
      <tr id="{{email}}">
        <td>{{first_name}} {{second_name}} {{last_name}}</td>
        <td>{{email}}</td>
        <td>{{cell_phone}}/{{phone}}</td>
        <td>{{points}}</td>
        <td>
            <button class="btn btn-edit" id="{{id}}" onclick="edit_customer(this.id,'{{id_companies}}')"><i class="fi-department fi-sr-pencil"></i></button>
            <button class="btn btn-danger" id="{{id}}" onclick="delete_customer(this.id,'{{id_companies}}','{{email}}')"><i class="fi-department fi-sr-trash"></i></button>
        </td>     
      </tr>
    {{/each}}
    </tbody>
  </table>


<script>
  async function delete_customer(idCustomer,idCompany,email){
      if(await questionMessage(`😱 Eliminar a ${email}`,'¿Quieres eliminar a este cliente?')){
        window.location.href = "/fud/"+idCompany+"/"+idCustomer+"/delete-customer";
      }
  }

  async function edit_customer(idCustomer,idCompany){
    window.location.href = "/fud/"+idCompany+"/"+idCustomer+"/edit-customer";
  }
</script>